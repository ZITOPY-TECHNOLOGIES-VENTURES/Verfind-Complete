import React from 'react';
export const AdminDashboard: React.FC = () => (
  <div style={{ textAlign:'center', padding:'40px', color:'var(--text-secondary)' }}>
    <p style={{ fontFamily:"'Fraunces',serif", fontSize:22, color:'var(--text-primary)', marginBottom:8 }}>Admin Control Panel</p>
    <p style={{ fontSize:14 }}>System-wide verification and management tools.</p>
  </div>
);
