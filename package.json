{
  "name": "verifind",
  "version": "2.0.0",
  "private": true,
  "description": "Verifind monorepo — Railway build entry point (backend serves built frontend)",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm ci --prefix backend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  }
}
